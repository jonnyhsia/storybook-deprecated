<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.jonnyhsia.storybook">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:name=".app.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoTitle">
        <activity
            android:name=".page.main.MainActivity"
            android:theme="@style/AppTheme.NoTitle.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".page.storydetail.StoryDetailActivity">
        </activity>
        <activity
            android:name=".page.pagenotfound.PageNotFoundActivity"
            android:theme="@style/AppTheme.NoTitle.PageNotTitle">
        </activity>
        <activity android:name=".page.auth.AuthActivity">
        </activity>
        <activity
            android:name=".page.writing.WritingActivity"
            android:windowSoftInputMode="stateAlwaysVisible|adjustResize">
        </activity>
    </application>

</manifest>